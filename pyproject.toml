[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "myalphabet"
version = "2026.2.10"
description = "An alphabet learning game for young children"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "Parent Developer"}
]
keywords = ["alphabet", "learning", "children", "game", "education"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Education",
    "Topic :: Games/Entertainment",
]
dependencies = [
    "pillow>=10.0.0",
    "pyyaml>=6.0",
    "python-vlc>=3.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "black>=23.0",
]

[project.scripts]
myalphabet = "myalphabet.__main__:main"
myalph = "myalphabet.__main__:main"

[project.gui-scripts]
myalphabet-gui = "myalphabet.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
myalphabet = ["*.yaml"]
